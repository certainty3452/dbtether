---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: restores.dbtether.io
spec:
  group: dbtether.io
  names:
    kind: Restore
    listKind: RestoreList
    plural: restores
    shortNames:
    - rst
    singular: restore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target.databaseRef.name
      name: Target
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.duration
      name: Duration
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Restore is the Schema for the restores API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RestoreSpec defines the desired state of Restore
            properties:
              onConflict:
                default: fail
                description: |-
                  How to handle conflicts with existing data
                  - fail: Abort if database is not empty (default)
                  - drop: Drop and recreate the database before restore
                  - overwrite: Restore over existing data (may cause conflicts)
                enum:
                - fail
                - drop
                - overwrite
                type: string
              source:
                description: Source of the backup to restore from
                properties:
                  backupRef:
                    description: Reference to an existing Backup resource
                    properties:
                      name:
                        type: string
                      namespace:
                        description: Namespace of the Backup (defaults to same namespace
                          as Restore)
                        type: string
                    required:
                    - name
                    type: object
                  latestFrom:
                    description: LatestFrom automatically finds the latest successful
                      backup for a database
                    properties:
                      databaseRef:
                        description: Reference to the Database to find the latest
                          backup for
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        type: object
                      namespace:
                        description: Namespace to search for backups (defaults to
                          same namespace as Restore)
                        type: string
                    required:
                    - databaseRef
                    type: object
                  path:
                    description: |-
                      Direct path to backup file in storage (e.g., "cluster/database/20260120-140000.sql.gz")
                      Requires storageRef to be set
                    type: string
                  storageRef:
                    description: Reference to BackupStorage (required when using path)
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              target:
                description: Target database to restore into
                properties:
                  databaseRef:
                    description: Reference to the Database to restore into
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - databaseRef
                type: object
              ttlAfterCompletion:
                description: Auto-delete after completion
                type: string
            required:
            - source
            - target
            type: object
          status:
            description: RestoreStatus defines the observed state of Restore
            properties:
              completedAt:
                format: date-time
                type: string
              duration:
                description: Duration of the restore operation
                type: string
              jobName:
                description: Name of the Job created for this restore
                type: string
              message:
                description: Human-readable message about the current status
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: Current phase of the restore operation
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              runId:
                description: RunID is a unique identifier for this restore run
                type: string
              sourcePath:
                description: Path of the backup file being restored
                type: string
              specHash:
                description: Hash of spec to prevent accidental re-runs
                type: string
              startedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
